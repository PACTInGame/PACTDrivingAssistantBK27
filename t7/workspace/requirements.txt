# Laufzeitabhaengigkeiten des Pruefstands.
#
# Python 3.11, Windows. Alles Weitere (socket, struct, ctypes, json) ist
# Standardbibliothek. Ohne pynput laesst sich das Modul importieren, aber die
# Fahrereingabe bleibt stumm.

pynput            # globale Tastaturerfassung (Pfeiltasten)

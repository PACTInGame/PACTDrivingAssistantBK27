[pytest]
# The suite must run on Linux, without LFS, without a display and without a
# network -- see reference/testing.md.
testpaths = tests
pythonpath = .
addopts = -q
filterwarnings =
    ignore:The asyncore module is deprecated:DeprecationWarning
